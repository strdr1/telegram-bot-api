# 📋 ИНСТРУКЦИЯ ПО ОБНОВЛЕНИЮ БОТА

## 🚀 БЫСТРЫЙ СТАРТ

### Автоматическое обновление (рекомендуется):
```batch
# Запустите из папки проекта
.\ssh_update.bat
# Введите пароль: Mashkov.Rest
```

### Ручное обновление (если скрипт не работает):
```bash
# 1. Подключиться к серверу
ssh root@155.212.164.61
# Пароль: Mashkov.Rest

# 2. Остановить сервисы
sudo supervisorctl stop telegram-bot-group

# 3. Обновить код
cd /opt/telegram-bot
sudo -u botuser git pull origin master --force

# 4. Запустить сервисы
sudo supervisorctl start telegram-bot-group

# 5. Проверить статус
sudo supervisorctl status
```

## 🔧 ЧТО ДЕЛАЕТ СКРИПТ ОБНОВЛЕНИЯ

### ✅ ШАГ 1: Подключение к серверу
- Проверяет доступность сервера
- Переходит в рабочую директорию

### ✅ ШАГ 2: Остановка сервисов
- Останавливает все процессы бота
- Ждет завершения процессов
- Проверяет что сервисы остановлены

### ✅ ШАГ 3: Обновление кода
- Скачивает последние изменения из Git
- Показывает последний коммит
- Применяет изменения кода

### ✅ ШАГ 4: Запуск сервисов
- Запускает все компоненты бота
- Ждет инициализации
- Проверяет статус

### ✅ ШАГ 5: Проверка работы
- Считает количество запущенных процессов
- Показывает последние сообщения в логе
- Подтверждает успешное обновление

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

### 🔴 НЕ РЕДАКТИРУЙТЕ КОД НА СЕРВЕРЕ НАПРЯМУЮ!
- Все изменения должны быть в Git репозитории
- Серверный код перезаписывается при обновлении

### 🔴 ЕСЛИ ОБНОВЛЕНИЕ ЗАВИСЛО:
```bash
# Проверьте логи
ssh root@155.212.164.61 "tail -50 /var/log/telegram-bot/bot.log"

# Принудительно перезапустите
ssh root@155.212.164.61 "sudo supervisorctl restart telegram-bot-group"
```

### 🔴 ЕСЛИ НУЖНО ОТКАТИТЬ:
```bash
# На сервере
cd /opt/telegram-bot
sudo -u botuser git reset --hard HEAD~1
sudo supervisorctl restart telegram-bot-group
```

## 🎯 ПРОВЕРКА РАБОТЫ ПОСЛЕ ОБНОВЛЕНИЯ

### Автоматическая проверка:
```bash
# Скрипт сам проверит в конце
curl https://a950841.fvds.ru/health
```

### Ручная проверка:
1. Напишите боту "привет"
2. Попробуйте забронировать стол
3. Проверьте админ-панель

### Логи работы:
```bash
ssh root@155.212.164.61 "tail -f /var/log/telegram-bot/bot.log"
```

## 📊 СТАТУС КОМПОНЕНТОВ

После успешного обновления должны работать:
- **telegram-bot** - основной бот
- **telegram-bot-scheduler** - планировщик обновлений меню
- **miniapp-api** - API для мини-приложения

## 🔍 ДИАГНОСТИКА ПРОБЛЕМ

### Если бот не отвечает:
```bash
ssh root@155.212.164.61 "ps aux | grep python"
ssh root@155.212.164.61 "sudo supervisorctl status"
```

### Если ошибки в коде:
```bash
ssh root@155.212.164.61 "tail -50 /var/log/telegram-bot/bot.log"
```

### Если проблемы с venv:
```bash
ssh root@155.212.164.61 "cd /opt/telegram-bot && source venv/bin/activate && python -c 'import aiohttp, aiogram; print(\"OK\")'"
```

## 🎉 ГОТОВО!

Теперь обновление бота - это просто запуск одного скрипта! 🚀
